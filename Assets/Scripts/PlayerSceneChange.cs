using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Valve.VR;
using UnityEngine.SceneManagement;
public class PlayerSceneChange : MonoBehaviour
{
    /*public GameObject Pointer;
     private void Awake(){
       
  
  string sIndex = SceneManager.GetActiveScene().name;
  if(sIndex!="menuscene"){
    GameObject[] playerDup = GameObject.FindGameObjectsWithTag("Player");
       foreach(GameObject dup in playerDup){
         if(this.gameObject == dup)
           Destroy(dup);
           else return;
       }
    Pointer.SetActive(false);
  }
  else Pointer.SetActive(true);
  }
  */

}
